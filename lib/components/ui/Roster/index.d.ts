import React from 'react';
import { BaseProps, FocusableProps } from '../Base';
export interface RosterProps extends BaseProps, FocusableProps {
}
export declare const Roster: React.FC<RosterProps>;
export default Roster;
